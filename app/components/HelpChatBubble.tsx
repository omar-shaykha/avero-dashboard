"use client";
import{useEffect,useRef,useState}from"react";import{Bot,Headphones,LoaderCircle,Send,X}from"lucide-react";
type Msg={id?:string;role:string;message:string;created_at?:string};
export default function HelpChatBubble(){const[open,setOpen]=useState(false),[messages,setMessages]=useState<Msg[]>([]),[text,setText]=useState(""),[busy,setBusy]=useState(false),[ready,setReady]=useState(false);const end=useRef<HTMLDivElement>(null);async function load(){const r=await fetch("/api/help-center/chat",{cache:"no-store"});if(r.status===401)return;setReady(true);if(r.ok){const d=await r.json();setMessages(d.messages||[])}}useEffect(()=>{load();const t=setInterval(load,15000);return()=>clearInterval(t)},[]);useEffect(()=>{end.current?.scrollIntoView({behavior:"smooth"})},[messages,open]);async function send(){const m=text.trim();if(!m||busy)return;setText("");setBusy(true);setMessages(x=>[...x,{role:"user",message:m},{role:"assistant",message:"عم استلم رسالتك وشغّل Rex Care Agent..."}]);const r=await fetch("/api/help-center/chat",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({message:m})});const d=await r.json().catch(()=>({}));setBusy(false);if(r.ok)await load();else setMessages(x=>[...x,{role:"assistant",message:d.error||"صار خطأ بالدعم"}])}if(!ready)return null;return <div className="fixed bottom-5 right-5 z-[80]"><button onClick={()=>setOpen(!open)} className="relative flex h-14 w-14 items-center justify-center rounded-2xl bg-cyan-400 text-slate-950 shadow-2xl shadow-cyan-500/25"><Headphones size={24}/><span className="absolute -right-1 -top-1 h-4 w-4 animate-ping rounded-full bg-emerald-400"/></button>{open&&<div className="absolute bottom-16 right-0 flex h-[520px] w-[360px] max-w-[calc(100vw-2rem)] flex-col overflow-hidden rounded-3xl border border-slate-800 bg-slate-950 text-white shadow-2xl"><div className="flex items-center justify-between border-b border-slate-800 bg-slate-900 p-4"><div className="flex items-center gap-3"><div className="rounded-xl bg-cyan-400/10 p-2 text-cyan-300"><Bot size={18}/></div><div><b>AVERO Care Agent</b><p className="text-xs text-slate-500">Rex يساعدك فوراً</p></div></div><button onClick={()=>setOpen(false)} className="rounded-lg p-1 text-slate-400 hover:bg-slate-800"><X size={18}/></button></div><div className="flex-1 space-y-3 overflow-y-auto p-4">{!messages.length&&<div className="rounded-2xl border border-slate-800 bg-slate-900/70 p-4 text-sm text-slate-400">اكتب مرحبا أو قلّي وين المشكلة، وأنا برد عليك من هون.</div>}{messages.map((m,i)=><div key={m.id||i} className={`flex ${m.role==="user"?"justify-end":"justify-start"}`}><div className={`max-w-[82%] rounded-2xl px-3 py-2 text-sm leading-6 ${m.role==="user"?"bg-cyan-500 text-slate-950":"border border-slate-800 bg-slate-900 text-slate-200"}`}>{m.message}</div></div>)}{busy&&<div className="flex items-center gap-2 text-xs text-cyan-300"><LoaderCircle className="animate-spin" size={14}/> Rex عم يكتب...</div>}<div ref={end}/></div><div className="border-t border-slate-800 p-3"><div className="flex gap-2"><input value={text} onChange={e=>setText(e.target.value)} onKeyDown={e=>{if(e.key==="Enter")send()}} placeholder="اكتب رسالتك..." className="flex-1 rounded-xl border border-slate-700 bg-slate-900 px-3 py-3 text-sm outline-none focus:border-cyan-400"/><button onClick={send} disabled={busy||!text.trim()} className="rounded-xl bg-cyan-400 px-3 text-slate-950 disabled:opacity-50"><Send size={18}/></button></div></div></div>}</div>}
