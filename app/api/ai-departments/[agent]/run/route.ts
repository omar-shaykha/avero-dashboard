import {createClient} from "@supabase/supabase-js";
import {canAccess,getAuthorizationContext,isKingAdmin,type FeatureKey} from "@/lib/auth/authorization";

const agents={
  sales:{feature:"ai_sales",manage:"sales.manage",key:"ai_sales",persona:"Leo"},
  marketing:{feature:"ai_marketing",manage:"marketing.manage",key:"ai_marketing",persona:"Foxy"},
  hr:{feature:"ai_hr",manage:"hr.manage",key:"ai_hr",persona:"Aero"},
  support:{feature:"ai_support",manage:"support.manage",key:"ai_support",persona:"Gor"},
  inventory:{feature:"ai_inventory",manage:"inventory.manage",key:"ai_inventory",persona:"Vexa"},
  "customer-care":{feature:"ai_customer_care",manage:"customer_care.manage",key:"ai_customer_care",persona:"Rex"},
  analytics:{feature:"ai_analytics",manage:"ai_analytics.manage",key:"ai_analytics",persona:"Nova"},
  warehouse:{feature:"ai_warehouse",manage:"warehouse.manage",key:"ai_warehouse",persona:"Bruno"}
} as const satisfies Record<string,{feature:FeatureKey;manage:string;key:string;persona:string}>;
type Agent=keyof typeof agents;
function db(){const u=process.env.NEXT_PUBLIC_SUPABASE_URL,k=process.env.SUPABASE_SECRET_KEY;if(!u||!k)throw new Error("Missing Supabase configuration");return createClient(u,k)}
async function generate(prompt:string){const key=process.env.GEMINI_API_KEY;if(!key)throw new Error("Missing Gemini configuration");const r=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent?key=${encodeURIComponent(key)}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({contents:[{parts:[{text:prompt}]}],generationConfig:{temperature:0.4}})});if(!r.ok)throw new Error("AI provider request failed");const j=await r.json();const text=j?.candidates?.[0]?.content?.parts?.map((p:{text?:string})=>p.text||"").join("").trim();if(!text)throw new Error("AI provider returned no content");return text}
async function callMake(url:string,payload:Record<string,unknown>){const response=await fetch(url,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});const text=await response.text();let parsed:unknown=text;try{parsed=text?JSON.parse(text):{};}catch{parsed={text};}if(!response.ok)throw new Error(`Make engine failed: ${response.status}`);return parsed}
export async function POST(request:Request,{params}:{params:Promise<{agent:string}>}){try{const {agent}=await params;if(!(agent in agents))return Response.json({error:"Unknown agent"},{status:404});const a=agents[agent as Agent],ctx=await getAuthorizationContext();if(!ctx)return Response.json({error:"Unauthorized"},{status:401});if(!(isKingAdmin(ctx)||canAccess(ctx,a.feature,a.manage)))return Response.json({error:"Forbidden"},{status:403});const companyId=ctx.profile.company_id;if(!companyId)return Response.json({error:"Company not configured"},{status:409});const body=await request.json();const task=String(body.task||body.brief||body.issue||"").trim().slice(0,12000);if(!task)return Response.json({error:"Task is required"},{status:400});const s=db();const [{data:profile,error:pErr},{data:config,error:cErr},{data:engine}]=await Promise.all([s.from("company_ai_profiles").select("industry,business_description,products_services,target_audience,brand_voice,languages,locations,social_notes").eq("company_id",companyId).maybeSingle(),s.from("ai_agent_configs").select("enabled,instructions,knowledge_scope,autonomy_mode").eq("company_id",companyId).eq("agent_key",a.key).maybeSingle(),s.from("ai_agent_engine_connections").select("make_webhook_url,enabled,status,make_scenario_id").eq("agent_key",a.key).maybeSingle()]);if(pErr||cErr)return Response.json({error:"Failed to load AI configuration"},{status:500});if(!config?.enabled)return Response.json({error:"AI department is disabled"},{status:409});const runInput={task,channel:body.channel||"dashboard",content_type:body.content_type||"manual_command",action:body.action||"manual_command",item:body.item||null,stock_data:body.stock_data||null,customer:body.customer||null,feedback:body.feedback||null,dataset:body.dataset||null,receiving_data:body.receiving_data||null,movement_data:body.movement_data||null};const makePayload={company_id:companyId,agent_key:a.key,agent_slug:agent,persona:a.persona,...runInput};
    if(agent!=="sales"&&engine?.enabled&&engine.make_webhook_url){try{const output=await callMake(String(engine.make_webhook_url),makePayload);return Response.json({ok:true,source:"make",agent:a.key,persona:a.persona,status:"approval_required",output})}catch(error){console.error("Make engine fallback:",error)}}
    const {data:run,error:runErr}=await s.from("ai_agent_runs").insert({company_id:companyId,agent_key:a.key,action:"manual_command",status:"running",input:runInput}).select("id").single();if(runErr)return Response.json({error:"Failed to start AI run"},{status:500});const prompt=`You are ${a.persona}, the ${a.key} agent for exactly one company.\nCOMPANY PROFILE:\n${JSON.stringify(profile||{})}\nAGENT INSTRUCTIONS:\n${config.instructions||""}\nKNOWLEDGE SCOPE:\n${config.knowledge_scope||"Use only this tenant's approved knowledge."}\nUSER TASK:\n${task}\nStrict rules: never use or infer another tenant's data; never invent prices, offers, stock, customers, candidates, integrations or capabilities. Follow the configured autonomy mode. Return the useful operational output with a clear next action.`;try{const output=await generate(prompt);const status=config.autonomy_mode==="approval"?"approval_required":"completed";await s.from("ai_agent_runs").update({status,output:{text:output,source:"internal"},completed_at:new Date().toISOString()}).eq("id",run.id);if(a.key==="ai_marketing")await s.from("marketing_content_queue").insert({company_id:companyId,channel:String(body.channel||"dashboard").slice(0,80),content_type:String(body.content_type||"post").slice(0,80),caption:output,status:"draft"});return Response.json({ok:true,source:"internal",agent:a.key,persona:a.persona,status,output,run_id:run.id})}catch(e){console.error(e);await s.from("ai_agent_runs").update({status:"failed",error_message:"AI generation failed",completed_at:new Date().toISOString()}).eq("id",run.id);return Response.json({error:"AI generation failed"},{status:502})}}catch(e){console.error(e);return Response.json({error:"Internal server error"},{status:500})}}
